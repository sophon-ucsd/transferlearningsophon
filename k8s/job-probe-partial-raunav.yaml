apiVersion: batch/v1
kind: Job
metadata:
  name: probe-partial-raunav
  namespace: cms-ml
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: probe-partial
        image: gitlab-registry.nrp-nautilus.io/escheuller/transfer-learning:cu121
        command: ["/bin/bash", "-c"]
        args:
        - |
          set -e
          git clone https://github.com/raunavm/transferlearningsophon.git
          cd transferlearningsophon
          pip install scikit-learn pyarrow

          mkdir -p /data/results/poster

          # Probe partial-FT-3M as the "finetuned" arg; pretrained as anchor.
          # Output goes to a separate CSV; merge with master probing_results.csv post hoc.
          PYTHONUNBUFFERED=1 python3 scripts/probe_observables.py \
            --observables /data/results/poster/observables_test_100k.parquet \
            --pretrained-dir /data/embeddings_test_20M \
            --finetuned-dir  /data/embeddings_partial_ft_3M_seed42_test100k \
            --finetune-label "partial_ft_3M_seed42" \
            --output /data/results/poster/probing_partial_results.csv \
            --n-per-class 10000 \
            --n-bootstrap 100 \
            --seed 42
        resources:
          requests:
            memory: "32Gi"
            cpu: "4"
          limits:
            memory: "32Gi"
            cpu: "8"
        volumeMounts:
        - name: pvc
          mountPath: /data
      volumes:
      - name: pvc
        persistentVolumeClaim:
          claimName: transfer-learning-vol
